# Legacy Public Images

This directory stores migrated legacy images that are referenced as static public URLs.

## Conventions

- Use stable, slug-like filenames.
- Group by domain area, for example `publications/`.
- Record source URL and usage in `specs/002-legacy-content-migration/media-inventory.md`.
